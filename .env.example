# ============================================================================
# LineSDK - Environment Variables Example
# ============================================================================
# This SDK supports two token modes. Choose one based on your needs:
#
# TokenMode Options:
#   - Static: Simple, use long-lived Channel Access Token (for development)
#   - Stateless: Auto-refresh JWT token (recommended for production)
# ============================================================================

# =========================
# Option 1: Static Mode (Development)
# =========================
# Simple setup using long-lived Channel Access Token from LINE Developers Console
# Pros: Easy to setup, no additional API calls
# Cons: Token becomes invalid if someone reissues from console

Line__TokenMode=Static
Line__ChannelAccessToken=YOUR_LONG_LIVED_CHANNEL_ACCESS_TOKEN
Line__ChannelSecret=YOUR_CHANNEL_SECRET

# =========================
# Option 2: Stateless Mode (Production)
# =========================
# Auto-refresh JWT token using Channel ID and Channel Secret
# Pros: No manual token management, auto-refresh before expiry
# Cons: Requires ChannelId configuration

# Line__TokenMode=Stateless
# Line__ChannelId=YOUR_CHANNEL_ID
# Line__ChannelSecret=YOUR_CHANNEL_SECRET

# =========================
# Optional Settings
# =========================
Line__BotBasicId=@YOUR_BOT_ID
Line__AddFriendUrl=https://line.me/R/ti/p/@YOUR_BOT_ID
Line__LiffRegisterUrl=https://liff.line.me/YOUR_LIFF_ID
